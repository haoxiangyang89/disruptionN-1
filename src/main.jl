# main program structure construction

function mainConstruct()
    # read in the data and construct the program
    return mainBody;
end

function solveMain(mainBody, printLog = false, iterNo = 1000)
    # readin main program and execute the SDDP algorithm
    
end
